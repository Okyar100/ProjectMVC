@model UserEditViewModel
@{
    ViewData["Title"] = "İstifadəçini düzəliş et";
}
<div class="panel">
    <form asp-action="Edit" asp-route-id="@Model.Id" method="post">
        @Html.Partial("_Form", Model)
    </form>
</div>
